# Smart Energy AI — runtime dependencies
#
# Every package here is imported by the project. Minimums are given rather than
# hard pins so the environment can move, with one exception noted below.

# Web application
Flask>=3.0
gunicorn>=21.0
python-dotenv>=1.0          # optional: app.py starts without it

# Data and ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.9           # the shipped model pickle was built with 1.9.0
joblib>=1.3                 # loads models/energy_forecast_model.pkl

# Explainability — /api/ml/explain
lime>=0.2.0.1

# PDF report — /api/report/pdf
reportlab>=4.0

# Knowledge service
requests>=2.31

# scikit-learn and the model pickle
# ------------------------------------
# models/energy_forecast_model.pkl was pickled with scikit-learn 1.9.0. Loading it
# under an older version raises InconsistentVersionWarning and the results are not
# guaranteed to match. Install 1.9 or newer, or retrain with run_pipeline.py under
# whatever version you are on.
